import styled from "styled-components";

export const Wrapper = styled.div`
  /* max-width: 80%; */
  color: var(--white);
  margin-bottom: 20px;

  h2 {
    margin-left: 12px;
  }

  .MuiInputBase-root {
    color: var(--white);
  }

  .leave-comment {
    margin: 10px;
  }

  .header {
    color: var(--white);
  }

  .userComment {
    padding: 0;
    margin: 0;
  }
`
